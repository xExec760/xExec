local Module = {}
Module.Name = "PrintTest"
Module.Extension = ".module"

Module.Execute = function(customArgument)
	if customArgument and customArgument ~= "" then
		print(customArgument)
	else
		print("USE MODULE")
	end
end

return Module
